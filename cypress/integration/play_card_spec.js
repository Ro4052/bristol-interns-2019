describe('Play card', () => {
    beforeEach(() => {
        cy.login('username');
        cy.startGame();
    });

    describe('on word appearing', () => {
        it('is able to play a card', () => {
            // TODO: Write test
        });
    });
});
